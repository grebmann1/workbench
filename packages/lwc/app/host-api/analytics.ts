/**
 * Analytics surface for extension Apps. Re-exports the shared analytics
 * default export so extensions can `import Analytics from 'host-api/analytics'`.
 */
export { default } from 'shared/analytics';
